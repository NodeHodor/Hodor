{
    "statements": [
        {
            "action": "*",
            "resource": [
                "/user/${user.id}"
            ]
        },
        {
            "action": "*",
            "resource": [
                 "/user/${user.id}/*"
            ]
        }
    ]
}
