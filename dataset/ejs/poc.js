// const fs = require("fs");
const ejs = require("ejs");
// fs.writeFileSync("aaa.txt", "<% process.mainModule.require('fs').writeFileSync('./ejs-success.txt', '23'); %>");
// console.log(ejs.renderFile("aaa.txt", {}, (e, str) => { console.log(e);console.log(str);}));
// fs.unlinkSync("tmp.txt");
ejs.renderFile("", () => {});
